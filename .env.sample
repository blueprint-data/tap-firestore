# Firebase/Firestore Configuration
TAP_FIRESTORE_PROJECT_ID=your-gcp-project-id
TAP_FIRESTORE_CREDENTIALS_PATH=/path/to/credentials.json

# Alternative: Use credentials as JSON string
# TAP_FIRESTORE_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key":"..."}'

# Alternative: Use credentials as base64-encoded string (recommended for CI/CD)
# Generate with: base64 -i credentials.json (macOS) or base64 -w 0 credentials.json (Linux)
# TAP_FIRESTORE_CREDENTIALS_BASE64=ewogICJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsC...
